# Gemini API key — used ONLY on the server (Vite dev middleware / Vercel
# serverless function). It is never exposed to the browser.
GEMINI_API_KEY=

# Supabase (existing project config — unchanged)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
